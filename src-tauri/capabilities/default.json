{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the grading workbench window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
